#!/usr/bin/env bash
if [ "$(uname)" = "Darwin" ] && [ "${BASH_VERSINFO[0]:-0}" -lt 4 ]; then
	exec /opt/homebrew/bin/bash "$0" "$@"
fi

. "$(dirname "$0")/ssh-context"

if $is_ssh; then
	_cached_val=$(tmux show-environment "TMUX_${pane_key}_SSH_USER" 2>/dev/null | sed 's/^[^=]*=//')
	if [[ -n "$_cached_val" ]]; then
		echo "$_cached_val"
		exit 0
	fi
fi

get_username() {
	local username=""
	if $is_ssh; then
		if grep -qwE 'cyberark\.cloud|coppel\.cloud' <<< "$ssh_host"; then
			username=$(ssh -qG "$ssh_host" | awk '/^user / {print $2}' | awk -F@ '{print $3}')
		else
			username=$(ssh -qG "$ssh_host" | awk '/^user / {print $2}')
		fi
	fi
	if test -z "$username"; then
		echo "${tty_commands[-1]}" | awk '{print $1}'
	else
		echo "$username"
	fi
}

result=$(get_username)
if $is_ssh; then
	tmux set-environment "TMUX_${pane_key}_SSH_USER" "$result"
fi
echo "$result"
